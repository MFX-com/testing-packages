{
  "$schema": "node_modules/lerna/schemas/lerna-schema.json",
  "version": "independent",
  "packages": [
    "packages/*"
  ],
  "changelogPreset": "./changelog-preset.config.json",
  "command": {
    "version": {
      "allowBranch": "main",
      "ignoreChanges": [
        "**/*.md",
        "**/*(\\.|-)test.ts"
      ]
    }
  }
}
